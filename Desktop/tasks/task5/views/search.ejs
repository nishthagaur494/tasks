<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
</head>
<body>
    <h1>Search</h1>
    <form action="/search" method="get">
        <input type="text" name="q" placeholder="Search for books, movies, or products" value="<%= query %>" required>
        <button type="submit">Search</button>
    </form>

    <% if (results && results.length > 0) { %>
        <h2>Results:</h2>
        <ul>
            <% results.forEach(result => { %>
                <li><%= result %></li>
            <% }); %>
        </ul>
    <% } else if (query) { %>
        <h2>No results found for "<%= query %>"</h2>
    <% } %>
</body>
</html>
